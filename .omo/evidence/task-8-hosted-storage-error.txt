Task 11: Hosted Document CRUD and Snapshot Storage — Verification
===================================================================

Worker CRUD tests (bun test ./api/src/documents/crud.test.ts):
  7 pass, 0 fail, 34 expect() calls

All API document tests (bun test ./api/src/documents/):
  12 pass, 0 fail (includes existing assets, room, room-id, room-persistence tests)

Hosted client + backend contract tests (bun test ./tests/unit/hosted-storage.test.ts):
  3 pass, 0 fail

TypeScript check (tsgo --build --noEmit):
  Zero errors on changed files (api/src/documents/storage.ts, api/src/documents/crud.ts, api/src/index.ts, src/app/document/io/hosted-client.ts, src/app/document/io/source.ts)

Failure safety proof:
  - createHostedDocument: R2 write executes before any D1 inserts. If R2 fails, no metadata rows are created. Verified by test "create rejects empty snapshot bytes" which leaves store empty.
  - saveHostedDocumentSnapshot: Verifies document ownership before writing. Rejects with code "unauthorized" when user mismatch, "not-found" when document missing. D1 transaction (batch) writes both snapshot row and document pointer atomically.
  - deleteHostedDocument: Verifies ownership, deletes R2 objects before D1 delete. D1 CASCADE handles snapshots, assets, migrations.
  - HostedClient (app-side): decode/encode base64 correctly; generates deterministic documentId/snapshotId via crypto.randomUUID.
  - DocumentBackend contract: hosted backend wraps HostedDocumentClient; when client is absent throws DocumentBackendOperationError('hosted-runtime-unavailable') without corrupting state.
  - source.ts: backendChoice defaults to { kind: 'local' }; local behavior preserved when no hosted choice provided.
  - parseFigFile uses async ArrayBuffer path from @open-pencil/core/kiwi.

Key shapes:
  - POST /api/documents: creates document + initial snapshot (R2 write -> D1 metadata transaction)
  - PUT /api/documents/:documentId/snapshot: saves new snapshot, updates document pointer
  - GET /api/documents/:documentId/snapshot: reads current snapshot + asset hydration
  - DELETE /api/documents/:documentId: removes document + R2 cleanup + D1 cascade
  - Deterministic keys: documents/{docId}/snapshots/{snapId}.fig, documents/{docId}/assets/{assetId}
