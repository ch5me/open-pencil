task-10: worker-auth session verification — error/failure scenario proofs

bun test api/src/auth.test.ts — 13 pass, 0 fail, 29 expect() calls

Error scenario proofs:
  - unauthenticated: no credentials → { type: 'unauthenticated' }
  - identity-conflict: cookie + bearer differ → { type: 'unauthorized', reason: 'identity-conflict' }
  - invalid-token: non-dev token → { type: 'unauthorized', reason: 'invalid-token' }
  - missing-session on /api/documents → 401 { error: 'unauthorized', reason: 'missing-session' }
  - missing-session on /api/documents/:id/room → 401 { error: 'unauthorized', reason: 'missing-session' }
  - /api/session returns { user: null, mode: 'unauthenticated' } for unauthenticated (not 401)
  - cookie + bearer same identity → accepted (idempotent, per auth-session-contract)
  - custom cookie name supported via opts parameter
  - protocol token (Sec-WebSocket-Protocol) parsed via bearer.<token> prefix

All 13 tests passing. 29 expect() calls cover all resolution paths.
