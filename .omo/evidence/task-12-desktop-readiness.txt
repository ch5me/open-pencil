# Task 15: Deferred Desktop Readiness Seams — Evidence

## Verification Run: 2026-05-29

### Oxlint (type-aware)
- Command: `npx oxlint -c oxlint.json --type-aware --type-check src/ packages/core/src/ packages/vue/src/ packages/cli/src/ packages/mcp/src/`
- Result: 0 warnings, 0 errors across 860 files
- All changed files clean

### Unit Tests
- Command: `bun test ./tests/unit/tauri-hosted-seams.test.ts`
- Result: 6 pass, 0 fail, 5 expect() calls
- All disabled-seam behaviors verified

### Full Test Suite
- Worker CRUD: 12 pass, 0 fail (api/src/documents/)
- App hosted-storage: 3 pass, 0 fail (tests/unit/hosted-storage.test.ts)
- Desktop seams: 6 pass, 0 fail (tests/unit/tauri-hosted-seams.test.ts)
- Combined: 21 pass, 0 fail

### Files Created
- `src/app/tauri/hosted.ts` — deferred desktop hosted-readiness seams
- `src/app/tauri/plugin-store.d.ts` — deferred type declarations for plugin-store
- `tests/unit/tauri-hosted-seams.test.ts` — unit tests for disabled seams

### Acceptance Criteria
- [x] Desktop-specific hosted auth/callback hooks are isolated behind disabled seams
  - `registerDesktopAuthCallback()` guarded by `isTauri() && isHostedAuthEnabled()`
  - Returns no-op cleanup when gate is off
- [x] Current desktop local-file experience remains unchanged
  - `resolveTauriOperatingMode()` returns `'local-file'` by default
  - `shouldShowHostedUI()` returns `false` when gate is off
  - All hosted functions short-circuit without touching Tauri APIs
- [x] The future bearer/native integration path is documented and code-located without ambiguity
  - `storeBearerToken()` / `readBearerToken()` / `clearBearerToken()` use deferred plugin-store import
  - `TauriOperatingMode` type declared with `'local-file' | 'hosted-docs' | 'hosted-collab'`
  - `DESKTOP_CALLBACK_SCHEME` constant defined for deep-link registration
