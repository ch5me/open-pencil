- Task 1 source audit: current hosted-readiness state is still local-first. `src/router.ts` exposes `/share/:roomId`; `src/app/collab/room.ts` and `src/app/collab/session.ts` pass freeform/random room IDs into Trystero and IndexedDB; `src/app/document/io/source.ts` / `source-state.ts` track local file handle, Tauri path, download name, and saved version only. Contract must treat these as local/source metadata, not hosted identities.
- Auth alignment for Task 1: hosted modes must follow cookie-first browser sessions with bearer fallback for native/API callers, and reject cookie+bearer identity conflict rather than silently preferring one transport.
- Task 5 IO audit: local file behavior fans out through `createDocumentSourceActions` -> save/write/autosave plus reload watchers; the safe seam is a backend object injected into read/save/autosave orchestration while leaving File System Access, Tauri fs, and browser download helpers local-only.
- Task 5 hosted seam: app-side contract can cover hosted open/save/saveAs/autosave/metadata through an injected `HostedDocumentClient`, but must throw `hosted-runtime-unavailable` until Worker/API task slices provide the client.

- Task 2 package audit: `ch5-packages/packages/auth/elf-auth-client` and `firefly-cloud/packages/elf-auth-client` diverged on the public user identity field (`kiloUserId` vs `fireflyUserId`). The only active unscoped package consumer found in scope was `firefly-cloud/services/session-ingest`; other Firefly `fireflyUserId` references are internal worker-utils/service storage contracts, not this federation package boundary.
- Task 2 consumer migration note: sub-app consumers must import `@ch5me/elf-auth-client`, verify RS256/JWKS tokens with `createElfVerifier`, and read `payload.elfUserId`; internal Firefly DB/DO variables can keep `fireflyUserId`/`firefly_user_id` naming when not public auth payload.
- Task 7 schema audit: `api/` only had local Wrangler state before this slice, so the hosted document model needed a committed D1 migration plus TS schema/domain files rather than extending an existing Worker CRUD layer.
- Task 7 migration seam: local source state currently exposes handle/path/download/display metadata only; hosted D1 migration provenance should store source kind/name/fingerprint as metadata and never derive `document.id`, R2 keys, or ownership from those local fields.
- Task 3 naming audit: authoritative public auth docs still contain legacy `Kilo` surfaces in `ch5-company/elf-auth-cutover-spec.md`, `ch5-platform-package-catalog.md`, and federation telemetry docs. Treat those as rename-matrix inputs, not OpenPencil runtime concepts to rename in this slice.
- Task 3 compatibility lesson: canonical package schema should stay strict on `elfUserId`; backwards compatibility belongs at issuer/ingress/credential readers as one-way read-legacy/write-ELF adapters.

- Task 2 Durable Object export: wrangler requires Durable Object classes to be both a named module export AND importable with a local binding in the entrypoint. `export { X } from './mod'` alone fails at runtime with `ReferenceError: X is not defined`; must use `import { X } from './mod'; export { X }`.
- Task 2 binding names: `.ch5/services.yaml` binding names (`DB`, `DOCUMENTS`, `ASSETS`, `DOCUMENT_ROOM`) are the canonical contract — wrangler.jsonc bindings must match exactly for all stages.

- Task 8 feature-flag audit: repo had zero existing feature-flag surface for hosted rollout. All environment detection was implicit (IS_TAURI, IS_BROWSER constants) with no hosted auth/docs/collab gating. Flags module resolves from explicit `OPENPENCIL_HOSTED_ENV` env var only — no hostname sniffing, no `window.location` checks, no `import.meta.env.MODE` fallbacks.
- Task 8 topology alignment: `config/hosted-topology.json` mirrors `.ch5/environments.yaml` environment ids and adds per-environment feature flag defaults, callback URLs, and API origins. Preview enables hostedAuth only (smoke test isolation); staging enables auth+docs (storage validation); production mirrors staging with collab deferred.
- Task 8 validation: `validateHostedConfig()` in `@open-pencil/core/hosted` catches 5 violation types: missing apiOrigin with hosted on, missing authCallbackUrl with hostedAuth on, hostedCollab without hostedAuth, hostedCollab without hostedDocs, hostedDocs without hostedAuth. 32/32 validation checks pass.
- Task 8 disabled-mode proof: when `OPENPENCIL_HOSTED_ENV` is unset, resolveEnv() returns `'local'` and all hosted flags resolve to false. No implicit environment guessing exists in the flags resolution path.
- Task 8 TS7053 fix: `resolveString` originally accepted `keyof HostedEnvironmentConfig` which includes `'env'`, but `ENV_DEFAULTS[env]` is `Omit<HostedEnvironmentConfig, 'env'>`. Fixed by narrowing key to `Extract<keyof HostedEnvironmentConfig, 'apiOrigin' | 'authCallbackUrl' | 'appUrl'>` — the only string config keys that actually exist in `ENV_DEFAULTS`.

- Task 9 consumer audit: `firefly-jwt-auth.ts` already imports from `@ch5me/elf-auth-client` and reads `payload.elfUserId`; `elf-auth-topology.md` and `AGENTS.md` already declare the canonical contract. The only doc misalignment was `firefly-cutover-runbook.md` line 129, where the device-auth smoke test showed `.fireflyUserId` in the public API response shape — updated to `.elfUserId`. The duplicate package directory (`firefly-cloud/packages/elf-auth-client/`) still exists but is empty (node_modules only); `elf-auth-topology.md` already declares it removed. Internal `fireflyUserId` references across services/DB/storage are implementation-private names preserved per AGENTS.md do-not-touch list, not public auth payload surfaces.

- Task 6 auth test import fix: `api/src/auth.test.ts` used `../auth` from within `src/` directory, which resolved to `api/auth.ts` (outside the package). Correct import is `./auth` — the api package has its own `node_modules` and runs tests independently from the Bun workspace root.
- Task 6 session resolution shape: `resolveSession` returns a discriminated union (`authenticated | unauthenticated | unauthorized`) — downstream handlers pattern-match on `.type` rather than checking truthiness, making conflict detection explicit and testable.
- Task 6 Hono middleware typing: `requireSession` uses `(c as any).set('userId', ...)` because Hono's `Context.set` requires a typed `Env` binding that the stub doesn't yet declare. When `@ch5me/elf-auth-client` wires in, the `Env` type should be updated to carry `userId: string`.
- Task 6 `/api/session` design: returns `{user: null}` for unauthenticated requests (not 401), letting the frontend distinguish "no session" from "auth error". Protected routes (`/api/documents`, `/api/documents/:id/room`) still return 401 via `requireSession()`.
- Task 6 cookie name: uses `ELF_JWT` constant (not `better-auth.session_token`). `ELF_JWT_COOKIE` exported for test/config override.
- Task 6 protocol token: `resolveSession` also reads `sec-websocket-protocol` header via `protocolToken()` — parses comma-separated values looking for `bearer.<token>` prefix. Conflict detection: if cookie, bearer, and protocol tokens don't all resolve to the same value → identity-conflict.
- Task 6 `optionalSession` removed: only `requireSession` middleware remains. `/api/session` endpoint uses `resolveSession` directly (not middleware) to return `{user: null}` for unauthenticated.
- Task 6 identity-conflict test fix: `accepts cookie + bearer when same identity (idempotent)` used `same-tok` as cookie value but `DEV_STUB_ELF_TOKEN` as bearer. Since `verifyElfToken('same-tok')` returns `null`, the resolver saw two different credential values and hit `identity-conflict`. Fixed by using `DEV_STUB_ELF_TOKEN` in both channels.
- Task 6 evidence: hosted route gating proved by 4 Playwright tests (local path available, unauthenticated `/hosted` redirects to `/` with red banner, authenticated `/hosted` stays at `/hosted` with green banner, `/hosted/:documentId` redirects to `/`). Auth session resolution proved by 13/13 bun:test passes.
- Task 6 dual-branch proof: unauthenticated path shows red banner "BLOCKED: /hosted → / (no session, local mode)" with URL at `/`. Authenticated path seeds test token via `window.openPencil.test.hostedAuthToken`, shows green banner "ALLOWED: /hosted loaded (valid session, local mode)" with URL at `/hosted`. Both branches proven in same test suite.
- Task 12 transport seam: the safe swap was at `src/app/collab/room.ts` and `src/app/collab/session.ts`, keeping Yjs maps, awareness payloads, graph sync, follow mode, and IndexedDB persistence intact while changing only the wire transport and connection metadata.
- Task 12 mode selection: local share still uses random `/share/:roomId` Trystero rooms; hosted collab uses `/hosted/:documentId`, derives the Durable Object room server-side from `document.id`, and seeds the browser WebSocket auth token through the test/browser bridge for local proof lanes.
- Task 12 Worker auth nuance: browser `fetch()` with a custom `Authorization` header triggers CORS preflight, so the Worker must explicitly allow `Authorization` in `allowHeaders` and return the caller origin string, not a static list, or hosted session bootstrap fails before collab even attempts to join.
- Task 12 WebSocket nuance: Playwright/browser clients sending `Sec-WebSocket-Protocol: openpencil-room.v1,bearer.<token>` require the Durable Object upgrade response to echo an accepted protocol (`openpencil-room.v1`) or the browser rejects the handshake even if the Worker otherwise upgrades successfully.
- Task 12 proof shape: multi-client hosted convergence is visible with two `/hosted/doc_test` contexts showing one peer avatar each, one remote cursor on the first client after the second client moves, and matching scene graph child counts after a rectangle is created on one client. Unauthorized join rejection remains a plain `401 {"error":"unauthorized","reason":"missing-session"}` at the Worker boundary.
