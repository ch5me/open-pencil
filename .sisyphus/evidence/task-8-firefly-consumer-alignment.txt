Task 9: Firefly Consumer Alignment — Verification

Runtime consumer (session-ingest middleware):
- firefly-cloud/services/session-ingest/src/middleware/firefly-jwt-auth.ts
  - Line 2:  import { createElfVerifier } from '@ch5me/elf-auth-client';
  - Line 6:  import type { ElfTokenPayload } from '@ch5me/elf-auth-client';
  - Line 93: const exists = await userExists(c.env, payload.elfUserId);
  - Line 98: c.set('user_id', payload.elfUserId);
  - Uses canonical @ch5me/elf-auth-client, reads payload.elfUserId

Auth topology doc:
- firefly-cloud/docs/elf-auth-topology.md (lines 88-97)
  - Canonical package source: ch5-packages/packages/auth/elf-auth-client
  - Canonical publish name: @ch5me/elf-auth-client
  - Public token payload identity field: elfUserId
  - Deprecated local duplicate declared removed
  - Consumer migration notes for session-ingest, cloud-agent, sub-apps

AGENTS.md federation claim:
- firefly-cloud/AGENTS.md (line 10)
  - "federate via @ch5me/elf-auth-client (RS256+JWKS, package hq-be8u)"

Cutover runbook public API response shape:
- firefly-cloud/docs/firefly-cutover-runbook.md (line 129)
  - Device-auth smoke test uses elfUserId (ELF naming), not fireflyUserId
  - Updated from .fireflyUserId to .elfUserId in this task

Duplicate package residue:
- firefly-cloud/packages/elf-auth-client/ — directory exists but contains only node_modules/
  - Actual package files removed; topology doc declares it deprecated/removed
  - No consumers import from the unscoped path

Stale naming audit:
- kiloUserId: 0 matches in firefly-cloud codebase
- Unscoped elf-auth-client imports: 0 matches (only @ch5me/elf-auth-client)
- fireflyUserId: present in internal storage/DB/service code — per AGENTS.md do-not-touch list, these are implementation-private names (firefly_users table, internal variables), not public auth payload surfaces

RESULT: PASS — all Firefly Cloud consumers and docs aligned to @ch5me/elf-auth-client contract and ELF naming.
